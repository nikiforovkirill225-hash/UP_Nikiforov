<Page x:Class="UP_Nikiforov.Pages.AuthorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UP_Nikiforov.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="✍️ Творческая мастерская" 
      Loaded="Page_Loaded"
      Background="#FFF5F7FA">

    <Page.Resources>
        <!-- Стиль для DataGrid -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFE2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeight" Value="50"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="AlternatingRowBackground" Value="#FFF9FAFB"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGrid">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14">
                            <ScrollViewer>
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FFF8F9FC"/>
            <Setter Property="Foreground" Value="#FF2C3E50"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="46"/>
            <Setter Property="BorderBrush" Value="#FFE2E8F0"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="14,0"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#FF3A4D61"/>
        </Style>

        <Style TargetType="DataGridCheckBoxColumn">
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>

        <!-- Стиль для кнопки добавления -->
        <Style x:Key="AddButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97" ScaleY="0.97"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Background="#FFF5F7FA" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Шапка страницы -->
            <Border Grid.Row="0" 
                    Background="White" 
                    CornerRadius="16,16,0,0" 
                    Padding="24,20" 
                    Margin="0,0,0,1"
                    BorderBrush="#FFE2E8F0"
                    BorderThickness="0,0,0,1">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="48" Height="48" CornerRadius="14" Background="#FFE94560" Margin="0,0,16,0">
                            <TextBlock Text="✍️" FontSize="26" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel>
                            <TextBlock Text="Творческая мастерская" FontSize="20" FontWeight="Bold" Foreground="#FF1A1A2E"/>
                            <TextBlock Text="Управление вашими произведениями" FontSize="12" Foreground="#FF7F8C9A"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Панель действий -->
            <Border Grid.Row="1" 
                    Background="White" 
                    CornerRadius="0,0,16,16" 
                    Padding="24,16" 
                    Margin="0,0,0,20"
                    BorderBrush="#FFE2E8F0"
                    BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="8" Height="8" CornerRadius="4" Background="#FFE94560" Margin="0,0,10,0"/>
                        <TextBlock Text="📚 Библиотека автора" FontSize="14" FontWeight="SemiBold" Foreground="#FF2C3E50"/>
                        <Border Background="#FFE8ECF2" CornerRadius="20" Padding="6,2,8,2" Margin="12,0,0,0">
                            <TextBlock x:Name="BooksCountText" Text="0 произведений" FontSize="11" Foreground="#FF5A6E7A"/>
                        </Border>
                    </StackPanel>

                    <Button x:Name="btnAddBook" 
                            Grid.Column="1"
                            Content="➕ Создать книгу" 
                            Style="{StaticResource AddButtonStyle}"
                            Background="#FF27AE60" 
                            Click="btnAddBook_Click"/>
                </Grid>
            </Border>

            <!-- Таблица с книгами -->
            <Border Grid.Row="2" CornerRadius="14" Background="White" BorderBrush="#FFE2E8F0" BorderThickness="1">
                <DataGrid x:Name="dgAuthorBooks" 
                          AutoGenerateColumns="False" 
                          IsReadOnly="True" 
                          SelectionMode="Single" 
                          MouseDoubleClick="dgAuthorBooks_MouseDoubleClick"
                          BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding ID}" Width="55"/>
                        <DataGridTextColumn Header="📖 Название произведения" Binding="{Binding Title}" Width="2.5*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="#FF1A1A2E"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="📄 Аннотация" Binding="{Binding Description}" Width="3.5*"/>
                        <DataGridCheckBoxColumn Header="🔒 Статус" Binding="{Binding IsFrozen}" Width="100">
                            <DataGridCheckBoxColumn.ElementStyle>
                                <Style TargetType="CheckBox">
                                    <Setter Property="IsHitTestVisible" Value="False"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridCheckBoxColumn.ElementStyle>
                        </DataGridCheckBoxColumn>
                    </DataGrid.Columns>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FFF0F7FF"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#FFE8F0FE"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                </DataGrid>
            </Border>

            <!-- Подсказка о двойном клике -->
            <Border Grid.Row="2" 
                    Background="Transparent" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Bottom" 
                    Margin="0,0,16,16">
                <Border Background="#FFF8F9FC" 
                        CornerRadius="20" 
                        Padding="12,6" 
                        BorderBrush="#FFE2E8F0" 
                        BorderThickness="1">
                    <TextBlock Text="💡 Дважды кликните по книге для редактирования" 
                               FontSize="11" 
                               Foreground="#FF7F8C9A"/>
                </Border>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>